<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ── Module category ─────────────────────────────────────────── -->
        <record id="module_category_multichannel" model="ir.module.category">
            <field name="name">Multichannel Sales</field>
            <field name="sequence">90</field>
        </record>

        <!-- ── Security groups ────────────────────────────────────────── -->

        <!--
            mc_user: Standard operator.
            Can view and manage channels, products, mappings, orders, and logs.
        -->
        <record id="group_mc_user" model="res.groups">
            <field name="name">Multichannel User</field>
            <field name="category_id" ref="module_category_multichannel"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!--
            mc_manager: Full control.
            Can configure channels, manage all records, and run pipelines.
            Implies mc_user.
        -->
        <record id="group_mc_manager" model="res.groups">
            <field name="name">Multichannel Manager</field>
            <field name="category_id" ref="module_category_multichannel"/>
            <field name="implied_ids" eval="[(4, ref('group_mc_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_admin'))]"/>
        </record>

        <!-- ── Record rules ────────────────────────────────────────────── -->

        <!-- Users see all channels (channels are global config, not per-user) -->
        <record id="rule_mc_channel_user" model="ir.rule">
            <field name="name">mc.channel: user access (all)</field>
            <field name="model_id" ref="model_mc_channel"/>
            <field name="groups" eval="[(4, ref('group_mc_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Managers have full write access to channels -->
        <record id="rule_mc_channel_manager" model="ir.rule">
            <field name="name">mc.channel: manager full access</field>
            <field name="model_id" ref="model_mc_channel"/>
            <field name="groups" eval="[(4, ref('group_mc_manager'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
