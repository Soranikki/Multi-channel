User: Fix the following issues. The issues can be from different files or can overlap on same lines in one file.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/main.py around lines 276 - 300, backfill_platform_orders currently awaits normalize_queue_and_dispatch serially for each order which blocks the event loop; change it to dispatch normalization/dispatch work concurrently by creating tasks for each order (use asyncio.create_task or gather) so the loop doesn't await each normalize_queue_and_dispatch sequentially, and ensure normalize_queue_and_dispatch and any DB/file work are run off the event loop (e.g., wrap blocking calls with asyncio.to_thread or make them fully async) to avoid blocking; also catch httpx.HTTPStatusError from resp.raise_for_status and re-raise as an HTTPException (e.g., 502) with a clear message so downstream errors from the platform orders endpoint are surfaced cleanly instead of producing an internal 500.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/main.py around lines 206 - 225, The code is performing blocking I/O inside async handlers (e.g., normalize_queue_and_dispatch calling store.append and event_store.queue_event), which blocks the event loop; change these synchronous file/DB calls to run in a worker thread using asyncio.to_thread or an executor and await their results (apply the same change for event_store.mark_* calls referenced in ws_odoo and for fail_integration_event), replacing direct calls to store.append, event_store.queue_event, and event_store.mark_* with awaited to_thread-wrapped wrappers so the coroutine yields while the blocking operations run off-thread.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/main.py around lines 264 - 273, The current broad except block turns any failure into a 400; narrow it so client-side errors return 400 and server/infrastructure errors return 5xx: catch and convert ValueError (and other explicit validation errors) to HTTPException(status_code=400), re-raise HTTPException unchanged, and for all other exceptions (e.g., failures inside normalize_queue_and_dispatch, queue_event, broadcast_to_odoo) raise an HTTPException with a 5xx code (500) or wrap/log the original exc before returning 500; adjust the try/except around the request.json() -> normalize_queue_and_dispatch(platform, event) flow in main.py to implement these specific catches and preserve useful logging.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/main.py around lines 129 - 138, In verify_webhook_signature, add a freshness check for the signed timestamp: parse the incoming timestamp (confirm whether mock services send epoch seconds or ISO-8601 and implement the matching parse), compute the absolute age against current UTC time, and reject requests whose age exceeds a configurable tolerance (e.g., an env var WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS defaulting to a small value like 300). Keep the existing HMAC verification but perform the timestamp presence and freshness validation before accepting the signature; raise an HTTPException (401/403) when the timestamp is missing or outside the allowed window. Ensure you use timezone-aware UTC comparisons and handle parse errors by rejecting the request.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/odoo_ws_connector/main.py around lines 60 - 61, The ack response for non-matching event types is missing event_id so drain_pending_events can't call the middleware ack/fail; update the early return in the handler (the branch that checks event_type != "normalized_order.ready") to include the incoming event_id (return {"event_type":"connector.ack","event_id": event_id,"received_at": utc_now()}) ensuring the variable event_id is available in scope so drain_pending_events can recognize and clear pending events.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_core/data/mc_integration_service_data.xml at line 7, The base_url field currently set to "http://localhost:8030" in the mc_integration_service data record is incorrect for containers (localhost is the Odoo container itself); change the value to the reachable connector hostname/URL used in your compose topology (e.g. "http://odoo-ws-connector:8021" or the actual service DNS/IP) so Odoo can reach the connector, and because this record is noupdate="1" also add a migration/update step (e.g. a data migration script or an XML/CSV update routine) to update existing installs instead of relying only on fresh installs; update the mc_integration_service data record (field name "base_url") and add the migration logic to ensure existing databases get the corrected URL.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_platform_integration/models/mc_channel.py around lines 76 - 85, The write method currently calls the blocking network helper _check_connectivity() when activating a channel; remove that synchronous call from mc_channel.write and instead set integration_enabled and a non-blocking default sync_status (e.g. 'pending' or 'unknown') and update last_sync_at as needed; then trigger connectivity verification outside the DB transaction (for example by queuing a background job / invoking an existing cron/task runner or a dedicated action method like check_connectivity_for_channel that calls _check_connectivity() asynchronously for the record(s)). Ensure mc_channel.write no longer performs network I/O and keep field updates to integration_enabled, sync_status, and last_sync_at only.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_platform_integration/models/mc_channel.py around lines 52 - 54, Replace the broad "except Exception" around the Odoo webservice call so network errors are caught explicitly and JSON decode errors are handled separately: catch requests.RequestException as exc (log via _logger.warning with exc_info=True) and add a separate except ValueError for resp.json() failures (also logging exc_info=True) instead of returning None silently; reference the url, resp, and _logger identifiers in the changed block so the Odoo connector call and its error handling are fixed.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_product_inventory/models/mc_stock_sync_queue.py around lines 27 - 42, The current claim_pending_for_connector (search then write) is not concurrency-safe; change it to lock rows with SELECT FOR UPDATE SKIP LOCKED when selecting candidates so concurrent connectors cannot pick the same records. Replace the plain self.search call in claim_pending_for_connector with a locking search (use the ORM search parameters for_update=True and skip_locked=True or run an explicit SELECT ... FOR UPDATE SKIP LOCKED via self.env.cr) to fetch the rows, then perform the same write({'state':'processing','last_attempt_at': now}) and return _to_connector_payload() on the locked records; keep using the claim_pending_for_connector method, records variable, and the write/_to_connector_payload flow.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_product_inventory/models/mc_stock_sync_queue.py around lines 27 - 42, claim_pending_for_connector currently only selects records in states 'pending' or 'error', so rows left in 'processing' after a crash never get reclaimed; update claim_pending_for_connector to also include stale 'processing' rows by adding a condition that includes ('state','=','processing') when last_attempt_at is older than a configurable timeout (e.g. now - N minutes) or by adding a combined OR that treats ('state','in',['pending','error']) OR (('state','=','processing') AND ('last_attempt_at','<=', cutoff)); after selecting, continue to write state='processing' and update last_attempt_at to now; use the existing field names (claim_pending_for_connector, state, last_attempt_at, next_retry_at) and make the timeout configurable or documented.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_product_inventory/models/mc_stock_sync_queue.py around lines 55 - 56, The backoff calculation in mc_stock_sync_queue.py uses delay_minutes = min(60, 2 ** min(attempt_count - 1, 5)) which makes the outer min(60, ...) dead because 2**5=32 < 60; update the exponent cap so a 60-minute ceiling can be reached (for example change the inner cap from 5 to 6 so 2**6=64 which will then be clamped to 60) or replace the expression with an explicit clamp like delay = 2 ** min(attempt_count - 1, MAX_EXP) and set MAX_EXP so 2**MAX_EXP >= 60; adjust the calculation around attempt_count, delay_minutes and record.attempt_count accordingly.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/event_store.py around lines 112 - 114, The dict is assigning received_at/normalized_at values that may be ISO strings (from normalize_queue_and_dispatch via utc_now()) or datetimes (now), causing mixed types for TIMESTAMPTZ binding; coerce both raw_event.get("received_at") and normalized_event.get("normalized_at") to timezone-aware datetimes before assignment in the event creation (e.g., use dateutil.parser.isoparse or datetime.fromisoformat + set UTC) and fall back to the existing now variable if parsing fails or value is None so that the values bound by the code in event_store.py are always Python datetime objects.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/event_store.py at line 152, The backoff calculation for delay_minutes never reaches the intended 60-minute cap because 2 ** min(attempt_count - 1, 5) tops out at 32; update the expression so the exponent can reach a value that yields >=60 (for example change the clamp from 5 to 6) or apply the cap after exponentiation. Concretely, replace the existing line that computes delay_minutes (using attempt_count) with something like min(60, 2 ** min(attempt_count - 1, 6)) or calculate 2 ** (attempt_count - 1) and then apply min(60, ...), ensuring the unique identifier delay_minutes and attempt_count are used to locate and update the code.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/event_store.py around lines 209 - 214, The build_event_store function currently uses a hardcoded fallback DATABASE_URL with embedded credentials; remove that hardcoded default and make the function fail fast when EVENT_DATABASE_URL is not set: read EVENT_DATABASE_URL from the environment without a default (or use os.environ to force presence), validate it's non-empty, and if missing raise a clear exception (e.g., RuntimeError/ValueError) with a descriptive message; then pass the resulting database_url into DurableEventStore as before so deployments cannot silently use baked-in credentials.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @integration_services/middleware/app/event_store.py around lines 86 - 101, The ON CONFLICT DO UPDATE currently preserves attempt_count, causing redelivered events to keep prior attempts; update the DO UPDATE SET clause to reset attempt_count when an event is being re-queued. Specifically, inside the ON CONFLICT block that updates integration_events (the same section that sets status, last_error, next_retry_at, etc.), add an assignment for attempt_count that sets it to 0 when the new/excluded row is being queued (mirror the status logic) but preserves the existing attempt_count for events already in terminal states (e.g., when integration_events.status IN ('delivered','processed')); reference the ON CONFLICT DO UPDATE block and the attempt_count column to locate where to add this conditional reset.

- Verify each finding against current code. Fix only still-valid issues, skip the rest with a brief reason, keep changes minimal, and validate.

In @my_addons/mc_platform_integration/tests/test_channel_lifecycle.py around lines 78 - 83, The test test_02_reactivate_shopee assumes the connector is live because reactivating via self.shopee.write({'active': True}) calls the model's connectivity check and sets sync_status to 'success' only if _check_connectivity/_call_connector_api returns True; update the test to avoid depending on a live connector by patching the Shop/Integration model connectivity method (e.g., patch type(self.shopee)._check_connectivity or _call_connector_api) to return True during the reactivation step (or set up a patcher in setUpClass) so the write({'active': True}) produces sync_status 'success', and apply the same change to the other reactivation test at 91-96.

GitHub Copilot: Optimized tool selection
